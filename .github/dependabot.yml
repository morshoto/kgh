version: 2
updates:
    - package-ecosystem: gomod
      directory: /
      schedule:
          interval: daily

    # Enable version updates for Docker in the backend directory (if applicable)
    - package-ecosystem: 'docker'
      directory: '/'
      schedule:
          interval: daily
